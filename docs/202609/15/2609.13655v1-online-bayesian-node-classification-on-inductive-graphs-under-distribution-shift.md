---
title: Online Bayesian Node Classification on Inductive Graphs under Distribution Shift
title_zh: 分布偏移下归纳图上的在线贝叶斯节点分类
authors: "Jinwen Xu, Gonzalo Mateos Buckstein, Qin Lu"
date: 2026-09-12
pdf: "https://arxiv.org/pdf/2609.13655v1"
tags: ["query:drug-ood-gen"]
score: 8.0
evidence: 面向分布偏移下新到节点的归纳泛化与不确定性校准
tldr: 针对演化图上节点分类需兼顾归纳泛化与校准不确定性，标准GNN一次训练难以满足分布偏移场景。该工作将贝叶斯最后一层置于确定性GNN编码器上，提出变分贝叶斯最后一层目标联合训练编码器与近似后验，并在测试时冻结编码器做在线Laplace更新。在五个分布偏移基准上，online GVBLL在精度与负对数似然上均最优，Cora提升达17个百分点，ogbn-arxiv提升达14个百分点。它把在线贝叶斯更新与归纳图学习结合，为安全敏感应用提供兼顾准确性与校准的方案。
source: arxiv
selection_source: fresh_fetch
tables_json: "[{\"url\": \"assets/tables/arxiv/2609.13655v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1851, \"height\": 770, \"label\": \"Table\"}]"
motivation: 演化图节点分类面临分布偏移，要求模型归纳泛化到新节点并提供校准不确定性，标准GNN一次训练难以满足。
method: 将随机最后一层参数置于确定性GNN编码器上，用变分贝叶斯最后一层目标联合训练编码器与近似后验；测试时冻结编码器并做在线Laplace更新。
result: 在五个分布偏移节点分类基准上，online GVBLL在准确率与负对数似然上均达最优；Cora提升17个百分点，ogbn-arxiv提升14个百分点，校准性也有竞争力。
conclusion: 该方法统一在线贝叶斯更新与归纳图学习，为分布偏移下安全敏感节点分类提供兼具准确性与校准性的方案。
---

## 摘要
在演化图上，节点分类器必须满足两个关键要求：在分布偏移下对新到达节点具有归纳泛化能力，以及为安全敏感应用提供校准的不确定性。标准图神经网络（GNN）通常只训练一次，无法满足这两个要求。我们通过将随机的最后一层参数置于确定性 GNN 编码器之上，来适配贝叶斯最后一层（BLL）模型，以进行不确定性量化。分类所需的类别 Softmax 似然破坏了高斯共轭性，因此无论是训练后验还是测试时的流式更新都不具有闭式解。为解决这两个挑战，我们引入变分贝叶斯最后一层（VBLL）目标，通过最大化带有蒙特卡洛期望对数似然的证据下界，联合训练编码器和近似最后一层后验。测试时，我们冻结编码器，并对最后一层后验应用在线拉普拉斯更新。该更新对应于一个幂先验贝叶斯模型，具有指数遗忘，并通过 Kullback-Leibler 散度锚定到训练后验。在分布偏移下的五个节点分类基准上，在线 GVBLL 是唯一在每个数据集上都取得最佳准确率和负对数似然的方法。相较于最强的非 GVBLL 基线，它将 Cora 上的准确率最多提升 17 个百分点，在 ogbn-arxiv 上最多提升 14 个百分点，同时在校准方面与 MC Dropout、Deep Ensembles、Temperature Scaling 和高斯过程分类器相比仍具有竞争力。

## Abstract
On evolving graphs, node classifiers must satisfy two key requirements: inductive generalization to newly arriving nodes under distribution shift and calibrated uncertainty for safety-sensitive applications. Standard graph neural networks (GNNs) are typically trained once and address neither requirement. We adapt the Bayesian last-layer (BLL) model by placing random last-layer parameters on top of a deterministic GNN encoder for uncertainty quantification. The categorical softmax likelihood required for classification breaks Gaussian conjugacy, so neither the training posterior nor the test-time streaming update has a closed-form solution. To address both challenges, we introduce a variational Bayesian last-layer (VBLL) objective that jointly trains the encoder and an approximate last-layer posterior by maximizing an evidence lower bound with a Monte Carlo expected log-likelihood. At test time, we freeze the encoder and apply an online Laplace update to the last-layer posterior. This update corresponds to a power-prior Bayesian model with exponential forgetting and a Kullback-Leibler anchor to the training posterior. Across five node-classification benchmarks under distribution shift, online GVBLL is the only method to achieve the best accuracy and negative log-likelihood on every dataset. It improves accuracy by up to 17 percentage points on Cora and 14 percentage points on ogbn-arxiv over the strongest non-GVBLL baseline, while remaining competitive in calibration with MC Dropout, Deep Ensembles, Temperature Scaling, and Gaussian-process classifiers.

---

## 论文详细总结（自动生成）

## 1. 核心问题与整体含义
- **研究背景**：图神经网络（GNN）是节点分类主流方法，但通常假设训练与测试节点同分布；真实演化图中，新节点常来自新研究领域、新用户社区或新攻击模式，导致分布偏移。
- **两大需求**：对训练时未见的新到节点具备**归纳泛化**能力；对安全敏感应用提供**校准的不确定性**。
- **现有方法不足**：
  - 标准 GNN 训练一次后参数固定，无法随数据分布漂移自适应，且 softmax 输出通常未校准。
  - 贝叶斯 GNN 对全网络做贝叶斯处理，代价高且分布偏移下需重训。
  - VBLL 在回归中依赖高斯共轭获得闭式更新，但分类的 categorical softmax 似然破坏共轭性，在线 VBLL 分类尚待解决。
  - MC Dropout、Deep Ensembles、Temperature Scaling 等通用 UQ 方法不携带跨流后验状态。
- **整体含义**：论文将贝叶斯最后一层（BLL）扩展到图上的在线分类，提出 GVBLL，在分布偏移流式场景中同时追求准确率、负对数似然与校准性。

## 2. 方法论
- **核心思想**：在确定性 GNN 编码器之上放置随机贝叶斯线性分类头；离线用变分训练联合学习编码器与最后一层近似后验；在线冻结编码器，仅用 Laplace + 幂先验更新轻量最后一层后验。
- **问题设定**：
  - 图 \(G_t=(V_t,E_t)\)，\(t=0\) 有标记训练节点，之后每步新到一批 \(\Delta V_t\)，需对其分类并给出校准不确定性。
  - 编码器 \(g_\theta\) 输出嵌入 \(z_v\in\mathbb{R}^{d_e}\)。
- **生成模型**：
  - 权重先验：\(w_c\sim\mathcal N(0,I_{d_e})\)，\(c\in[C]\)。
  - 分类似然：\(y_v|z_v,W\sim \mathrm{Cat}(\mathrm{softmax}(z_v^\top W/\tau_v))\)。
  - 每节点温度：\(\tau_v=\mathrm{softplus}(h_{\theta_\tau}(z_v))>0\)，由共享 MLP 实现。
- **变分训练**：
  - 近似后验为逐类因子化高斯：\(q(W)=\prod_c \mathcal N(w_c;\mu_c,\mathrm{diag}(\sigma_c^2))\)，参数为 \(M,\Sigma\)，共 \(2d_eC\) 个。
  - 负 ELBO：期望对数似然项 + KL 正则项；KL 权重 \(\alpha_{\mathrm{KL}}\) 从 0 线性退火到 1。
  - 因 categorical softmax 使期望对数似然不可解析，用重参数化采样 \(W^{(s)}=M+\Sigma^{1/2}\odot E^{(s)}\) 做 Monte Carlo 估计。
  - 两阶段训练：Phase 1 离线联合训练编码器、温度头和 \(M,\Sigma\)；Phase 2 冻结编码器与温度头，仅在线更新 \(M,\Sigma\)。
- **预测推断**：
  - 贝叶斯预测无解析式，用 MC 平均 softmax 近似。
  - 点预测与 NLL 使用 MAP 估计，避免后验平均带来的系统性软化。
- **在线更新**：
  - 精确贝叶斯更新因非共轭不可解，采用 Laplace 近似保持因子化高斯形式。
  - 引入幂先验：\(q_{t+1}(W)\propto p(D_t|W)[q_t(W)]^\lambda \exp(-\frac{\beta}{2}\sum (W-M^*)^2/\Sigma^*)\)。
  - \(\lambda\in[0,1)\) 控制指数遗忘，有效记忆约 \(1/(1-\lambda)\) 批；\(\beta\) 为 KL 锚强度，限制偏离训练后验。
  - 闭式更新：精度 \(T=1/\Sigma\) 按 \(\lambda T^{(t)}+\eta\sum \hat p(1-\hat p)z^2+\eta\beta/\Sigma
