---
title: "Graph Learning on Ensembles of Cyclic Peptides: An Investigation of Molecular Ensemble Modeling"
title_zh: 环肽集合的图学习：分子集合建模研究
authors: "Aaron Feller, Kris Deibler, Maxim Secor"
date: 2026-07-23
pdf: "https://arxiv.org/pdf/2607.21561v1"
tags: ["query:mgp"]
score: 8.0
evidence: 在环肽分子集合上进行分子图预训练，使用自监督任务
tldr: 分子性质预测常依赖单一构象，但溶液中的分子以构象集合形式存在。为利用这一信息，本文提出EnsembleEGNN模型，用等变图神经网络编码各构象，再通过Set Attention池化为整体表示，并在环肽数据集上多任务预训练。实验表明，预训练模型在环肽性质预测上显著优于仅用序列的BERT基线（R²从0.439提升到0.477），与BERT联合训练更达0.538。这证实了编码构象集合的热力学信息能有效提升预测性能。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.21561v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1400, \"height\": 583, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.21561v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 811, \"height\": 431, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.21561v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 833, \"height\": 500, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.21561v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 833, \"height\": 1012, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.21561v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1213, \"height\": 247, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.21561v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 857, \"height\": 278, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.21561v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1703, \"height\": 1595, \"label\": \"Table\"}]"
motivation: 现有分子性质预测忽略构象集合，而溶液分子以ensemble存在，需引入热力学加权信息。
method: 提出EnsembleEGNN，用共享EGNN编码各构象，Set Attention池化为整体表示，结合掩码恢复、坐标与距离重建联合预训练。
result: 在环肽数据集上，预训练模型R²=0.477，优于序列BERT的0.439；与BERT联合训练达0.538，Pearson r=0.737。
conclusion: 编码构象ensemble为热力学加权嵌入可显著提升环肽性质预测。
---

## 摘要
基于结构的分子性质预测通常使用单一代表性构象，尽管许多分子在溶液中以构象集合形式存在。我们提出了EnsembleEGNN，一种分子集合基础模型，它通过首先使用共享的等变图神经网络层对每个构象进行编码，然后通过集合注意力块对所得构象表示进行池化来编码集合。我们在环肽集合数据集CREMP上使用结合掩码标记恢复、噪声坐标重建和成对距离重建的多任务自监督目标对模型进行预训练。在CREMP-CycPeptMPDB数据集上，从头训练EnsembleEGNN完全失败（$R^2=0.005$）。然而，预训练模型达到了$R^2=0.477$和皮尔逊相关系数$r=0.699$，优于仅基于序列的BERT基线（$R^2=0.439$，皮尔逊$r=0.667$）。当EnsembleEGNN与BERT序列编码器进行端到端联合训练时，混合模型进一步提高到$R^2=0.538$和皮尔逊$r=0.737$。这些结果表明，将构象集合编码为单一热力学信息嵌入可改善环肽性质预测。

## Abstract
Molecular property prediction from structure often uses a single representative conformation, even though many molecules exist as conformational ensembles in solution. We introduce EnsembleEGNN, a molecular ensemble foundation model that encodes an ensemble by first encoding each conformer with shared Equivariant Graph Neural Network (EGNN) layers, then pooling the resulting conformer representations with a Set Attention Block. We pretrain the model on CREMP, a cyclic peptide ensemble dataset, using a multi-task self-supervised objective combining masked token recovery, noisy-coordinate reconstruction, and pairwise distance reconstruction. On the CREMP-CycPeptMPDB dataset, training EnsembleEGNN from scratch fails entirely ($R^2=0.005$). However, the pretrained model reaches $R^2=0.477$ and Pearson $r=0.699$, outperforming the sequence-only BERT baseline ($R^2=0.439$, Pearson $r=0.667$). When EnsembleEGNN is co-trained end-to-end with the BERT sequence encoder, the hybrid model improves further to $R^2=0.538$ and Pearson $r=0.737$. These results demonstrate that encoding conformational ensembles into a single thermodynamically informed embedding improves cyclic-peptide property prediction.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：当前分子性质预测方法通常只使用单一代表性构象（conformation），但溶液中的分子实际上以玻尔兹曼加权的构象集合（conformational ensemble）形式存在。对于柔性分子（如环肽），这种表示不匹配会丢弃关键的热力学信息，导致预测不准确。
- **研究动机**：现有的结构基方法要么处理单个构象，要么对构象做简单池化（如平均），无法有效捕捉原子在不同可及状态下的运动。随着构象生成管线的进步，能够编码整个集合的模型将变得愈发重要。
- **整体含义**：本文旨在证明，将构象集合显式地编码为单个热力学信息嵌入可以显著提升环肽性质预测性能，从而提出一种新的分子基础模型设计原则。

## 2. 论文提出的方法论：核心思想、关键技术细节、公式或算法流程

- **核心思想**：采用“分别编码 + 集合池化”范式。每个构象先通过共享的等变图神经网络（EGNN）独立编码，再通过一个集合注意力块（Set Attention Block, SAB）将各构象的表示聚合成一个统一的整体嵌入。
- **关键技术细节**：
  - **数据表示**：每个肽由原子标识符（\(a_i\)）、多构象坐标（\(X^{(c)}\)）和玻尔兹曼权重（\(w_c\)）组成。从CREMP数据集提取前5个（\(C=5\)）权重最高的构象，权重重归一化。
  - **构象编码**：每个构象通过 \(L\) 层EGNN处理，更新节点特征和坐标，消息传递仅在构象内部的 \(k\)-最近邻图上进行，保证对旋转和平移的等变性。
  - **构象融合**：最终的节点特征 \(\tilde{h}^{(c)}_i\) 通过注意力机制聚合得到每原子嵌入 \(h^{\text{ens}}_i\) 和全局嵌入 \(z^{\text{ens}}\)。注意力权重结合了学习得分和玻尔兹曼权重（公式2，使用\(\log w_c\)作为先验偏置）。然后通过Set Attention Block（使用诱导点，复杂度 \(O(C \cdot M)\)，\(M\)为诱导点数）将全局嵌入 \(z^{\text{ens}}\) 聚合出来。
- **预训练目标**：多任务自监督损失，包含三项（权重0.3/0.5/0.2）：
  - 掩码标记恢复（交叉熵，类似BERT）
  - 噪声坐标重建（MSE）
  - 成对距离重建（MSE，比较由每原子嵌入投影得到的距离与玻尔兹曼加权平均结构的距离）
- **算法流程**：见论文Algorithm 1：输入原子类型、构象坐标、权重 → 初始化原子特征 → 经过L层EGNN更新 → 计算每个构象的全局表示 \(\bar{h}^{(c)}\) → 计算注意力权重 \(\alpha_c\) → 生成 \(h^{\text{ens}}_i\) 和 \(z^{\text{ens}}\)。

## 3. 实验设计：数据集、benchmark、对比方法

- **数据集**：
  - 预训练：CREMP（Conformer-rotamer ensembles of macrocyclic peptides），包含环肽的构象集合。
  - 下游微调：CREMP-CycPeptMPDB，含有 \(n=3003\) 个实验测量的人工膜渗透性（PAMPA）分数。去除边界值后保留 \(n=2979\)。
- **Benchmark**：预测 \(\log(P_{\text{app}})\)（膜渗透性对数），评估指标：MAE、RMSE、Pearson相关系数 \(r\)、\(R^2\)。
- **对比方法**：
  - BERT-only：仅使用序列的PeptideCLM-2编码器。
  - EnsembleEGNN-random-init：几何模型，随机初始化，不预训练。
  - EnsembleEGNN：预训练后的几何模型。
  - Hybrid：EnsembleEGNN与BERT联合训练，拼接嵌入。
- **实验协议**：5折交叉验证，固定划分。3折训练、1折验证、1折测试。每个模型在3个独立随机重复下报告均值±标准误。

## 4. 资源与算力

- **文中未明确说明**使用的GPU型号、数量、训练时长。仅在补充表S1中列出了超参数：
  - 预训练：80 epochs，batch size 8，学习率 \(2\times10^{-4}\)。
  - 微调：20 epochs，batch size 8，学习率 \(1\times10^{-4}\)（几何模型提前停止耐心3个epoch）。
  - 模型参数量：EGNN backbone 7.85M，构象融合 3.42M，总计约11.3M参数。BERT编码器为114M参数。
- 未提及节点算力资源，无法评估训练成本。

## 5. 实验数量与充分性

- **实验数量**：主要实验包含4种模型对比，5折交叉验证，3次独立重复，共 \(4\times5\times3=60\) 次下游微调运行（实际上表1为3次重复均值）。此外还提供了预训练损失曲线和折叠详细R²表。
- **消融实验**：通过随机初始化对比，验证了预训练的必要性（\(R^2=0.005\) vs \(0.477\)）。通过Hybrid对比，验证了联合序列信息的增益。
- **充分性与公平性**：
  - 优点：使用相同的数据划分和微调协议，实验设计较严谨。但**仅在一个下游任务（PAMPA）上评估**，缺乏多个不同性质（如结合亲和性、毒性等）的验证。数据集规模较小（~3000），泛化能力存疑。
  - 对比基线：BERT基线是强基线（114M参数），且EnsembleEGNN参数更少（11.3M），性能超越BERT，凸显几何信息的价值。
- 整体上实验设计良好但覆盖有限，未涉及其他分子类型或更大规模数据集。

## 6. 论文的主要结论与发现

- 从头训练几何模型（随机初始化）完全失败（\(R^2=0.005\)），**预训练对于在有限数据上有效微调是必要的**。
- 预训练后的EnsembleEGNN显著优于仅序列的BERT基线（\(R^2=0.477\) vs \(0.439\)，Pearson \(r=0.699\) vs \(0.667\)）。
- 与BERT联合训练的Hybrid模型达到最佳性能（\(R^2=0.538\)，Pearson \(r=0.737\)），表明序列信息与几何集合信息互补。
- 以较小的模型规模（11.3M）获得比大模型（114M BERT）更好的表示，证明显式编码构象集合的优势。

## 7. 优点：方法或实验设计上的亮点

- **方法创新**：将构象集合作为基础模型的输入，首次在环肽领域实现“集合感知”的几何预训练。引入玻尔兹曼加权注意力，整合热力学先验。
- **效率设计**：使用Set Attention Block的诱导点机制，使构象融合复杂度线性于构象数量，而非二次；EGNN的局部消息传递保证等变性且可扩展到任意分子大小。
- **预训练任务巧妙**：结合token、坐标、距离三种任务，其中距离重建目标使用玻尔兹曼加权平均结构而非简单平均，保持了热力学意义。
- **实验对比清晰**：通过随机初始化消融明确证明了预训练的必要性，通过Hybrid证明了几何与序列的互补性。

## 8. 不足与局限

- **架构局限**：当前EGNN仅在单个构象内传递消息，没有跨构象的消息传递，可能限制捕捉构象间协同信息。
- **数据局限**：仅使用CREMP预训练和CycPeptMPDB微调，数据集较小（~3000），且只针对环肽一种分子类型，通用性未知。需要更多下游任务（如结合亲和力、溶解度等）以及更多分子类型验证。
- **潜在偏差**：CREMP构象集由计算生成，其质量和覆盖范围影响模型学习。未讨论构象数量k=5的选择敏感性。
- **泛化风险**：结果基于特定数据集和任务，跨论文比较需谨慎。未评估模型对实验构象噪声的鲁棒性。
- **未报告算力与时间**：缺少GPU型号、训练时间等关键信息，影响可复现性评估。

（完）
